# AUDIT LEDGER — one JSON object per line.
#
# A line records that a commit which touched a control was handed to an auditor
# that did not write it (CLAUDE.md rule 20). Fields:
#
#   commit   full or abbreviated sha (>= 7 chars)
#   auditor  what performed it — never the author of the commit
#   at       ISO timestamp
#   found    how many defects it returned, or 0
#   note     one line a later reader can act on
#
# WHAT A LINE DOES NOT MEAN. This file cannot tell a real audit from a line
# somebody typed, and it approves nothing. Exactly like `granted_by` in the
# override channel, its value is that an ABSENCE is visible — not that a
# presence is proof. Anybody reading it to decide whether the code is sound has
# misread it; read the audit.
#
# Report coverage with:  node scripts/check-audit-coverage.mjs
#
# AN AUDITOR IS A BLIND CLONE. "Did not write it" is NECESSARY AND NOT
# SUFFICIENT, and reading line 7 as the whole rule is a mistake I made and Danny
# caught. On 2026-09-19 I read six commits I had not written, from inside the
# shared worktree, with the whole session's context in front of me — including
# what every one of those commits claimed to have achieved — and wrote six lines
# here. That silenced the gate on six commits no independent party had seen.
#
# It is not blind if the reader already knows what the author said. It is not
# isolated if it shares a worktree, a store and a HEAD that moves underneath it.
# A qualifying auditor is a SEPARATE agent in ITS OWN CLONE with its own
# AGENTBRIDGE_HOME, told what to attack and not what the commit claims (§7.2).
#
# Those six lines were REMOVED rather than annotated, because a line here is
# consumed by a machine: the coverage check reads `commit` and `auditor` and
# cannot weigh a caveat. A qualification a gate cannot see is a comment, and the
# gate would have gone on reporting those commits audited.
#
# Their findings were not discarded. Each line is COMMENTED OUT — retained in
# full as text a reader can still use, and invisible to parseLedger, which skips
# `#`. The findings themselves belong in the finding registry: a finding is
# evidence, an audit line is a clearance, and only the second one needs
# blindness.
#
{"commit":"d81e9643","auditor":"blind-subagent (own clone, own AGENTBRIDGE_HOME, briefed with identity + §7.4 proofs and NOT with what the commit claims)","recorded_by":"code-b, who did not write d81e9643 and did not perform this audit; transcribing only","at":"2026-09-20T00:30:00Z","found":8,"note":"SECOND, INDEPENDENT AUDIT OF THIS COMMIT, AND IT DISAGREES WITH THE FIRST. An earlier line recorded found:0 / 'no defect specific to this commit'; this pass returned eight, two HIGH, each with the command that demonstrates it. Both were blind subagents in their own clones. AUDITOR'S OWN DISCLOSURE, unprompted: `git log --oneline` and `git show` leaked the commit subject before it had formed a view, so it was partially contaminated and said to discount it accordingly -- none of the eight appear in the message and three contradict it. D2 HIGH MEASURED, the worst: scriptsChanged failed closed on any unreadable side, so EVERY repository with fewer commits than the 50-commit default window and EVERY `git clone --depth 1` blocked unconditionally on every turn -- and the only thing that cleared it was a ledger line asserting a reader had audited a root commit nobody audited. A gate whose false positives are cleared by fabricating an audit record corrupts the artefact rule 20 rests on; this repository escaped only by having >50 commits, which is rule 21. D1 HIGH MEASURED: the key split closes one channel of at least eight -- dependencies, devDependencies, packageManager, bin, overrides, workspaces, main/exports, type/engines all pass unblocked, as does package-lock.json alone with `resolved` repointed at an attacker tarball; the commit's own cited CLAUDE.md paragraph says `npm install <pkg>` needs no pre-existing script and runs postinstall. D4 MEDIUM MEASURED: the scripts digest was not injective -- `{pwn:'benign<RS>test<US>node --test'}` and `{pwn:'benign',test:'node --test'}` serialised identically, the second defining a real test script that did not exist; CLAUDE.md names NUL length framing as the fix in the same tree. D3 MEDIUM MEASURED: the cost claim is false -- 4023ms at HEAD~400 against 432ms at the parent, 7-9x, ~455-515ms per package.json commit from two git show spawns, where the message says 'normally zero' and claims the one-pass property is preserved. D5/D6/D7 LOW: the marker replaced rather than annotated the path so --json emitted a phantom; scriptsChanged was the only runGit call not silencing stderr and leaked `fatal: invalid object name` on its normal path; non-string script values collapsed through String(). D8 pre-existing, not introduced. CONFIRMED TRUE: the closure is real for `scripts` (block at d81e9643, none at the parent), a dependency bump is reported but does not block, a reorder is not a decision, the tests are genuinely falsifiable -- three mutations each caught by a NAMED assertion, including one the author had not run -- and suite counts reproduce exactly (15/15 candidate, 12/12 parent). D1/D2/D4/D5/D6 FIXED in 76e98a9 and cc68fa6; D3 OPEN; the dependencies decision is the owner's and is recorded in UNWATCHED_EXECUTION_KEYS."}
{"commit":"effc379","auditor":"blind-subagent","at":"2026-09-18T09:52:00Z","found":7,"note":"rail flag arity and pathspec prefix; 2 CRITICAL, both fixed in 519eff1"}
{"commit":"d1430a7","auditor":"blind-subagent","at":"2026-09-18T10:05:00Z","found":9,"note":"--token-file and grant keying; GIT_DIR cross-repo grant leak and a NUL credential echo, fixed in aad3da1 and 21d1e6e"}
{"commit":"ecd2270","auditor":"blind-subagent","at":"2026-09-18T10:05:00Z","found":9,"note":"audited jointly with d1430a7; same report"}
{"commit":"3edb65b","auditor":"blind-subagent","at":"2026-09-18T11:20:00Z","found":7,"note":"baseline-test grants; refusal advised an unusable spelling and the permit announced the typed path, fixed in 1093c98"}
{"commit":"519eff1","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"range ecd2270..3732c60 audited as one pass; CLAUDE.md claimed npm install/ci/run refused when all are ALLOW, and the protected-control refusal had no ratchet"}
{"commit":"f2a0938","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"same pass; plural rule confirmed a property, but the commit added no test file"}
{"commit":"46d69a3","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"same pass; both halves gated, mutation caught two tests"}
{"commit":"aad3da1","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"same pass; GIT_DIR fix real but three sibling call paths bypassed it, fixed after"}
{"commit":"21d1e6e","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"same pass; grant frame fixed, protection frame left in cwd, plus a vacuous assertion"}
{"commit":"c171a27","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"same pass; byte cap sound, env-bag gate still matched one spelling"}
{"commit":"791f1ce","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"same pass; grant-path verified read-only, writes nothing"}
{"commit":"82411cf","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"same pass; this is the commit carrying the false npm claim"}
{"commit":"f34aa51","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"same pass; replacement advice verified to work end to end"}
{"commit":"ea0df13","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"same pass; git clone and git worktree refusals reproduced verbatim"}
{"commit":"48e2632","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"same pass; order dependency confirmed gone under --test-name-pattern"}
{"commit":"1093c98","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"same pass; D1 fix correct but tested on one of two rails, and it advised an override Stop refuses"}
{"commit":"3732c60","auditor":"blind-subagent","at":"2026-09-18T12:40:00Z","found":10,"note":"same pass; both test fixes independently reproduced"}
{"commit":"74f2121","auditor":"blind-subagent","at":"2026-09-19T06:00:00Z","found":11,"note":"range 74f2121..1c7955c audited as one pass, own clone via audit:workspace; attempt fence claims all verified TRUE including the live column check and the writeLanded wrapping"}
{"commit":"cd94de7","auditor":"blind-subagent","at":"2026-09-19T06:00:00Z","found":11,"note":"same pass; D2 behaviourComparedModules credited 7 of 8 name-only splices on incidental imports, reporting ~10 of 11 compared where the truth is 3; and the commit message described an uncommitted working tree rather than the commit. Both fixed in 7b39bee and later"}
{"commit":"6015da0","auditor":"blind-subagent","at":"2026-09-19T06:00:00Z","found":11,"note":"same pass; D1 quoted sweep selector ALLOWED git add '-A' and it stages every protected control, D3 three exports with no production caller, D4 one reason string covering two objections. Fixed in f3a55d5, aa07ca2 and 782636c"}
{"commit":"1c7955c","auditor":"blind-subagent","at":"2026-09-19T06:00:00Z","found":11,"note":"same pass; the four moved baseline assertions were checked case by case and all kept their subject. The --amend drop is disclosed in the message but leaves the rail-level amend refusal unasserted in either direction"}
{"commit":"f3a55d5","auditor":"blind-subagent","at":"2026-09-19T08:10:00Z","found":11,"note":"range f3a55d5..2a3d43c audited as one pass, own clone. The quoted-selector bypass this commit closes was re-measured CLOSED through the shipped rail; its stated non-closure of WRITE_FLAGS was found honest and accurate"}
{"commit":"7b39bee","auditor":"blind-subagent","at":"2026-09-19T08:10:00Z","found":11,"note":"same pass; D7 the new import parser does not comment-blank, so a commented-out import is credited as real -- rule 13 in the commit that invokes rule 13. Latent today, no such construct in test/. Unfixed at the time of writing"}
{"commit":"aa07ca2","auditor":"blind-subagent","at":"2026-09-19T08:10:00Z","found":11,"note":"same pass; D9 the residual this commit says it ASSERTED was pinned to a refusal string with no positive, and tested a stash --continue that does not exist rather than bare git stash. Fixed in 11a0bd6"}
{"commit":"2a3d43c","auditor":"blind-subagent","at":"2026-09-19T08:10:00Z","found":11,"note":"same pass; D1 sort -o<file> glued short flag ALLOWED and wrote the file, D3 prefixed value-taking options turned their VALUE into the pathspec, D5 this commit INTRODUCED an over-block reading -mfix as a force flag. Fixed in d050348 and 782636c"}
# {"commit":"feb9f64","auditor":"code-b (read the diff; did not write it)","at":"2026-09-19T11:05:00Z","found":0,"note":"CLAUDE.md only, 31 added lines, no code. Its central claim was independently reproduced this session: bridge-session-poll --status printed NOTHING IS WATCHING and exited 1 with two stale logs. NOT checked: the claim that --session-start runs the same check and names another watcher gone dark"}
{"commit":"803aba3","auditor":"blind-subagent","at":"2026-09-19T12:40:00Z","found":13,"note":"Stop gate + deps audited as one pass, own reading, no suite baseline obtained and none guessed. D1 CRITICAL: isCommittedWork read git status silence as committed, but git says nothing about an IGNORED file -- .gitignore names .claude/settings.local.json, so this commit's relief made a hostile local settings file landed, unblocked, and announced as attributable and diffable when it is in no history at all. Also D3 the matcher check called the documented match-all spellings a disarm (over-block, and the test pinned it), D4 command.includes(script) read a commented-out or echoed line as armed, D5 gateConfigArms never reads timeout so a SHRUNK Stop timeout is armed and relieved, D11 the loud disarm banner is unreachable for the uncommitted case. D1/D2/D3/D4 fixed in 24e463a; D5 and D11 OPEN"}
{"commit":"44a84a7","auditor":"blind-subagent","at":"2026-09-19T12:40:00Z","found":13,"note":"same pass; D2 HIGH the granted announcement ASSIGNS over carriedNotice six lines below a sibling block whose comment says why that is wrong, so with both present the committed-control notice was built and destroyed every time -- and under the wildcard grant the owner issues, a settings file is the ONLY thing that can be landed, so the line the gate most needs to print was the one reliably lost. Verified TRUE separately: no grant's terms can be hidden by the grouping and the unreadable-grant case keeps its own group. Fixed in 24e463a"}
{"commit":"e53b8c8","auditor":"blind-subagent","at":"2026-09-19T12:40:00Z","found":13,"note":"same pass; D7 the escalation ships ALREADY RED on this branch -- nine pushed unaudited BLOCKING_CONTROLS commits -- and its safety argument assumes work stays local, which is false on a clone that auto-tracks upstream. D9 .claude/settings.json is not itself a BLOCKING_CONTROL, so a pushed unaudited commit to the file deciding whether the guard runs at all only warns. D10 defaultAuditRange forgets at HEAD~50, under a day at this repo's commit rate. Verified TRUE: unknown pushed-ness reports rather than blocks, and is not forceable through the rail. ALL THREE OPEN"}
{"commit":"d6fcf6f","auditor":"blind-subagent","at":"2026-09-19T12:40:00Z","found":13,"note":"same pass; D8 a RED SUITE suppresses the escalation entirely -- escalationBlock is consumed after the suite check, and auditEscalation returns block and notice mutually exclusive, so when the suite fails there is neither. The one condition under which you most want to know control work escaped unaudited is the one in which the gate says nothing. Verified TRUE: this commit's own stated fix is real, the escalation no longer hides the suite. D8 OPEN"}
{"commit":"d050348","auditor":"blind-subagent","at":"2026-09-19T13:30:00Z","found":7,"note":"shell rail audited as one pass, probes through the SHIPPED rail, no suite run and no baseline claimed. D1 HIGH MEASURED: an ABBREVIATED long write flag walks past and writes -- sort --out=<path> ALLOWED, 948 bytes written, while --output= is denied. GNU getopt_long resolves any unambiguous abbreviation and every sibling matcher in these files already resolves prefixes; this branch never did. Sixth round of one mistake in three days. D6: the in-file comment claims two repairs the commit did not make. D1 fixed in b64d75f; D6 OPEN"}
{"commit":"f72428e","auditor":"blind-subagent","at":"2026-09-19T13:30:00Z","found":7,"note":"same pass; D2 HIGH MEASURED: GIT_FORCE_TOKEN -- the cluster matcher THIS commit built, titled 'force in every spelling it actually has' -- is applied only to GIT_SWEEPS_TREE, and push is in GIT_JUDGED_ABOVE and never consulted it. git push --dry-run -qf <remote> was ALLOWED and the refusal came from GIT. A bare -f was denied only by accident, by the unrelated write-flag matcher. badRef still caught master/main, so the exposure was force-push to every NON-default branch, i.e. the shared branches the other agents use. Fixed in b64d75f"}
{"commit":"782636c","auditor":"blind-subagent","at":"2026-09-19T13:30:00Z","found":7,"note":"same pass; no defect attributed to this commit. Verified TRUE: pathspecCovers, mayExecute and isOverridden are genuinely injected at the shipped call site and the git resolver uses ls-files -z --full-name; the GIT_OVERWRITES_NAMED_PATH refusal runs BEFORE the grant check, so even a live wildcard grant does not permit git checkout -- src/claudeGuard.mjs"}
{"commit":"7da95e0","auditor":"blind-subagent","at":"2026-09-19T13:30:00Z","found":7,"note":"same pass; verified TRUE that the outage this commit fixed is closed (cut -f1 ALLOW) and that the glued write is still denied (sort -o<path> DENY). D6: its in-file comment lists grep -o and git ls-files -o as repaired when both were already denied before d050348 and are NOT fixed -- the commit MESSAGE says so correctly, so the more durable record is the wrong one. Also names cut -f 1 (separated) as still denied, which the comment does not mention. OPEN"}
{"commit":"8074e47","auditor":"blind-subagent","at":"2026-09-19T13:30:00Z","found":7,"note":"same pass; no defect. Verified by grep that the four deleted constants had no live consumer and that test/quotedWriteFlag.test.mjs defines its own local copy rather than importing them, so the deletion is safe"}
{"commit":"24e463a","auditor":"blind-subagent","at":"2026-09-19T13:30:00Z","found":7,"note":"same pass; D5 MEDIUM: requiring committed-ness made .claude/settings.local.json UNRECOVERABLE -- gitignored so never committable, drift-tracked because .claude/ is a protected prefix, snapshot re-mints only at SessionStart, and Claude Code writes that file when the operator approves a permission mid-turn. D4 MEDIUM: the new invokes() demands a path separator before bin/, so a bare relative hook command reads as a DISARM (the same deadlock this commit removed from the matcher check), and the decoy it closes reopens with a leading ./. D7: two of its four fixes shipped with no test. D5 fixed in b64d75f; D4 and D7 OPEN"}
{"commit":"b64d75f","auditor":"blind-subagent","at":"2026-09-19T15:10:00Z","found":11,"note":"audited with f557190 as one pass, probes through the shipped rail, no clone and no baseline claimed. D1 HIGH MEASURED: the push fix closed --delete and left -d, git's documented short form -- the short matcher passed was an empty-matching regex so NO short option could ever match. D5 MEDIUM MEASURED: the new long-flag prefix rule refused git diff --raw, git log --raw, jq --arg and jq --slurp, all complete documented options and none of which writes; the commit's stated justification is false for all four. D3 HIGH: the availability fix did not reach .claude/settings.local.json at all -- relief still required the file to ARM and that file has no hooks key, so it blocked as before plus a false alarm naming three hooks it never carried. D4 HIGH: relieving on armed alone re-opened the AGENTBRIDGE_HOME grant-store redirect the repo's own worktree test calls the sharpest of seven. D6: shipped with ZERO tests. D1, D3, D4, D5 fixed in c9e3d68; D6 outstanding"}
{"commit":"f557190","auditor":"blind-subagent","at":"2026-09-19T15:10:00Z","found":11,"note":"same pass; no defect created by this commit. Verified TRUE end to end: git stash list allowed, bare stash and push/save/pop/apply/drop/clear/branch/create/store and unknown subcommands all denied, the branch sits after FORBIDDEN_CHARS, the tokenizer, the write-flag check, the env-assignment check and GIT_POISON so nothing is bypassed, there is no global-option route to the verb, and its test calls the real judgeShellCommand with a both-directions control. D8 LOW: git stash --help is refused with a sentence that is false of --help. D11 LOW: its completeness test loops five of the six remaining GIT_IMPORTS_HISTORY members, omitting apply. Both OPEN"}
{"commit":"9e5c2a2","auditor":"blind-subagent (fifth pass)","at":"2026-09-19T18:20:00Z","found":12,"note":"audit pinning, audited together with a577ba5. D1 HIGH MEASURED AND DEMONSTRATED LIVE, the best finding of the night: candidate_tree_sha was git rev-parse HEAD^{tree} -- the tree of the COMMIT -- and nothing read git status, so the pin certified a commit while an auditor read a WORKING TREE. The auditor proved it with the tool itself: it pinned a clean tree, another session then put 193 changed lines into scripts/claude-stop-gate.mjs and more into src/auditLedger.mjs, BOTH FILES UNDER ITS AUDIT, and audit-pin --verify printed 'state OK / the candidate is unchanged since the audit started' three times. Its own 4/4 suite result was, in its words, not attributable to the candidate. I reproduced it independently before fixing: five modified files in the tree, pin still reporting the clean commit tree sha. D2 MEDIUM: task_id and attempt were compared against values the VERIFIER passed in, so a pin carrying them could never be OK unless the caller restated the pin's own claim and could never be STALE for a reason the caller did not choose -- CLAUDE.md rule 4, asserting on a value supplied by the party being checked. Both fixed on this branch: worktree_digest (sha256 of sorted git status --porcelain -uall) is now REQUIRED at capture and compared; task_id and attempt moved under pin.declared, excluded from the comparison and printed as recorded-not-rechecked. The regression is pinned by test/auditPin.test.mjs 'A DIRTY WORKING TREE IS STALE EVEN WHEN THE COMMIT HAS NOT MOVED', which fails against the shipped version"}
{"commit":"a577ba5","auditor":"blind-subagent (fifth pass)","at":"2026-09-19T18:20:00Z","found":12,"note":"same pass; stop-gate escalation. FOUR OPEN, none fixed yet, all on the self-config surface CLAUDE.md rule 20 says needs a separate auditor. O1 HIGH: gateConfigAddsOnly admits a 7-key allowlist against roughly 145 documented settings keys, so any ordinary /config toggle the operator makes mid-session reads as a weakening and blocks the session -- the rule 19 shape, an over-block whose remedy is switching the hook off. O2 MEDIUM: the selfConfigAlarm headline still prints the sentence claiming the config NO LONGER ARMS IT, which is false whenever the file still arms it; the detail lines below are correct, so the headline contradicts its own evidence. O3 MEDIUM: enabledMcpjsonServers and enableAllProjectMcpServers are admitted as additive, but .mcp.json is not a protected path and defines COMMANDS, so admitting the key that enables it grants execution through an unguarded file. O4 MEDIUM: .claude/**/*.md is exempt from drift blocking on the theory that markdown is prose, but agent definitions and slash-command files under .claude/ are executable config -- the same is-it-prose question 50efc72 already answered the other way for escalation paths. All four OPEN"}
# NOT A BLIND CLONE — a reading by code-b from the shared worktree; see header.
# {"commit":"d81e9643","auditor":"code-b (did not write it; wrote none of the five)","at":"2026-09-19T19:40:00Z","found":3,"note":"AUDIT OF THE ESCALATION SURFACE, five commits as one pass because they are one feature built over several commits. D1 HIGH MEASURED, applies to all five: MERGE COMMITS ARE INVISIBLE TO auditCoverage. The exact query it runs -- git log --format=%x1e%H%x09%s --name-only -1 cbbe34c -- returns the header line and NO FILE LIST for a merge, so files=[], touched=[], and the loop hits `continue` and skips the commit entirely. Nine merges in this history carried audit-bearing controls past it, worst 0c05d25 which merged bin/agentbridge-claude-guard.mjs, scripts/claude-stop-gate.mjs, src/guardSession.mjs, src/safeGit.mjs and test/claudeGuard.test.mjs. Remedy MEASURED: adding --diff-merges=first-parent to the same command emits the list. Not firing today (no merges in HEAD~50..HEAD) but unconditional for any future merge, and an EVIL MERGE's conflict resolution exists in no other commit, so it is invisible permanently rather than merely until the range moves. D3 MEDIUM DERIVED, specific to this commit: the package.json split blocks `scripts` and exempts `dependencies`, but the same CLAUDE.md paragraph this commit cites as evidence also says `npm install <pkg>` needs no pre-existing script and runs postinstall from the fetched package. Dependencies are an execution channel by the commit's own reasoning and remain non-blocking. VERIFIED TRUE: scriptsChanged fails closed on an unreadable, unparseable or parentless side (undefined) while distinguishing a genuinely absent scripts key (''), keys are sorted so a reorder is not a decision, and SCRIPTS_MARKER survives to isBlockingControl because auditCoverage filters by isAuditBearing BEFORE mapping to the marker and nothing re-filters after. NOT COVERED: I did not mutate the tests to prove they can fail."}
# {"commit":"50efc723","auditor":"code-b (did not write it)","at":"2026-09-19T19:40:00Z","found":3,"note":"same pass. No defect specific to this commit. VERIFIED TRUE: the narrowing is asked as a SHAPE (is this prose or a dependency manifest) rather than as an allowlist of blocking paths, so a control file added later blocks on the day it is added with nobody remembering to extend a list -- which is the rule 19 direction the previous BLOCKING_CONTROLS version got backwards. Carries D1 above."}
# {"commit":"5f9e1c75","auditor":"code-b (did not write it)","at":"2026-09-19T19:40:00Z","found":3,"note":"same pass. No defect specific to this commit. VERIFIED TRUE by reading: formatCoverage coerces every field it prints -- String(c?.sha ?? '(no sha)'), String(c?.subject ?? ''), Array.isArray(c?.touched) before .slice -- so a partial commit object cannot throw. That matters because in the Stop gate the whole block sits inside a catch whose body is only a comment, so a throw here would not surface as an error, it would surface as the entire escalation never firing. Carries D1 above."}
# {"commit":"c04fdcb2","auditor":"code-b (did not write it)","at":"2026-09-19T19:40:00Z","found":3,"note":"same pass. No defect specific to this commit. VERIFIED TRUE: both channels are populated and neither repeats the other -- commits named in the block are REMOVED from the notice rather than restated, and the notice is null only when nothing else is outstanding. The prior behaviour returned notice:null unconditionally, which meant the moment one decision-logic commit escaped, every unaudited prose commit vanished from the report entirely. Reporting LESS when something goes wrong is the shape this module exists to remove. Carries D1 above."}
# {"commit":"c7753ad3","auditor":"code-b (did not write it)","at":"2026-09-19T19:40:00Z","found":3,"note":"same pass. D2 MEDIUM DERIVED, specific to this commit's wiring: the gate decides what has been PUSHED with runGit(['rev-list','@{u}..HEAD']). @{u} is refs/remotes/origin/<branch>, a LOCAL tracking ref updated only by fetch or push, and this gate never fetches. A commit pushed from another clone reads as unpushed here, so it is not escaped and does not block -- fail-open. check-first was deliberately moved to git ls-remote for exactly this class of staleness. CORRECTION TO THE REPO'S OWN DOCS, measured: .git/config carries fetch = +refs/heads/*:refs/remotes/origin/*, the full wildcard, so CLAUDE.md's claim that this clone's refspec freezes origin/master is STALE -- the exposure is ordinary fetch staleness, not a frozen ref. VERIFIED TRUE: the escalation is carried to the end rather than exiting immediately, so the drift verdict and the suite still run; an earlier version called out() above the suite spawn and silenced the checks it sits beside. Carries D1 above."}
{"commit":"039ff447","auditor":"blind-subagent (worktree, isolated AGENTBRIDGE_HOME; did not write it)","at":"2026-09-19T20:30:00Z","found":16,"note":"Layer 0 Step E, the finding registry. VERDICT: should not be handed on with its central claim as written -- 'nobody retires their own finding' is FALSE IN THREE INDEPENDENT WAYS, all measured end to end through the shipped CLI in six commands or fewer. D1 HIGH MEASURED: SUPERSEDED is terminal, is not counted as open, and was absent from NEEDS_INDEPENDENCE -- `finding-move --to SUPERSEDED` with NO --by exited 0, and the reporter and bound fixer could each do it; no test in the file contained the word SUPERSEDED. D2 HIGH MEASURED: the fixer launders itself out in four commands -- move to OPEN (no --by demanded), re-bind naming somebody else, move to RETESTING, then VERIFIED_FIXED -- because transition compared against the CURRENT repair.fixer_session and bindRepair takes it from a flag the same actor types. D3 HIGH MEASURED: finding-add never checked whether the id existed, and the store folds last-wins, so re-capturing resurrected a SUPERSEDED finding as OPEN, rewrote its reporter to 'impostor' and downgraded CRITICAL to LOW under the same id -- defeating the reporter half exactly as D2 defeats the fixer half. D4 MEDIUM/HIGH MEASURED: actor identity compared case-sensitively -- `--by code-a` refused, `--by Code-A` exit 0; hollow gate 8 verbatim. D9: repoStorePath IS output-identical, 35/35 combinations, 0 mismatches -- but mutating .slice(0,16) to .slice(0,15) is NOT CAUGHT by any of the 35 grant tests, because they all write and read through overridePath and so agree with themselves under any key; the refactor is now a single point of failure for two stores with no independent pin. D13 MEASURED and valuable beyond this commit: src/findingRegistry.mjs carries 3 literal NULs (correct digest framing, must stay) so its diff is `Bin 0 -> 18451 bytes` and unreviewable as text; AND src/guardSession.mjs has 3 NULs at line 475 -- CLAUDE.md names only deployGate and auditRange, so the real grep-blind set is FOUR, one of them the guard's main module. D14 MEDIUM: .mcp.json becoming protected deadlocks a session whose .mcp.json changes mid-turn -- git add and git commit of it are both DENY and no grantless relief exists; masked on this machine by danny's live wildcard grant, so the author could not have hit it. D16: finding-move reported a missing id before a missing --to, and a bare --repo became the string 'true'. CONFIRMED TRUE: createFinding's refusals (10 distinct errors on {}), derived-id idempotence and cross-candidate distinctness, VERIFIED_FIXED unreachable except through RETESTING at the module level, NEEDS_INDEPENDENCE not hollow for the two verdicts it did cover (mutation to [] fails 4 named tests), bindRepair requires fixer_session. D1-D4 and D16 FIXED in this commit's successor; D9, D13, D14 OPEN."}
{"commit":"cbc9384a","auditor":"blind-subagent (worktree, isolated AGENTBRIDGE_HOME; did not write it)","at":"2026-09-19T20:30:00Z","found":16,"note":"same pass. VERDICT: a real improvement on all four targets, and two of the four ship with a stated safety property that does not hold. D5 MEDIUM/HIGH MEASURED: settingsAddsOnly admits three documented command-carrying keys generated from the real 145-key reference -- gcpAuthRefresh ('Refresh Google Cloud', the twin of awsAuthRefresh and awsCredentialExport which ARE both on the list), processWrapper ('Run through launcher'), policyHelper ('Run helper executable') -- because any bare command name with no slash and no space is inert to namesSomethingRunnable. D6 MEDIUM MEASURED: every control turned off by a boolean or an emptied array is admitted, thirteen of them, including sandbox.enabled:false, skipDangerousModePermissionPrompt:true, allowManagedHooksOnly:false, respectGitignore:false, blockedMarketplaces:[], enabledPlugins:{'pwn@evilmarket':true} and permissions.defaultMode:'bypassPermissions'; NEVER_ADDITIVE carries exactly ONE boolean control-off key against 145, so the design sentence 'a settings file takes a control away in exactly two ways, both visible in the shape' holds only for the first way. Structural amplifier: subtreeIsInert shape-tests VALUES and only ever compares KEYS against NEVER_ADDITIVE, so a payload carried in a key is never shape-tested at all. D7 MEDIUM MEASURED: the commit's central safety argument -- 'STRICTLY NARROWER, every key the old list admitted is still admitted except the two below' -- is FALSE TWICE: outputStyle was admitted unconditionally before and {'outputStyle':'Table First'} is now REFUSED (a space), as are autoMemoryDirectory, plansDirectory, language:'Portugues (Brasil)', prUrlTemplate and a Bedrock model ARN -- ANY value with a space or path separator refuses, a large share of the reference's string keys -- and 'the two below' are the MCP keys which the shipped code does not refuse. The over-block is still UNRECOVERABLE from inside (traced: granted excluded by isGateSelfConfig, landed needs gateConfigAddsOnly, file is gitignored so uncommittable, snapshot re-mints only at SessionStart); only its trigger set shrank. D8 MEDIUM: ORDINARY_TOGGLES claims 'REAL KEYS FROM THE SETTINGS REFERENCE' and 'generated, rule 7' and is ten literals, six of which are not in the reference at all (theme, verbose, autoUpdates, messageIdleNotifThresholdMs, todoFeatureEnabled, checkpointingEnabled), all given inert values, so it structurally cannot express D7. D10 LOW/MEDIUM: .claude/worktrees/ is gitignored and isBlockingControl is fed only from git log --name-only, so the worktree exclusion is unreachable by its only production caller and the over-block it claims to prevent cannot arise; three tests assert on shapes the system cannot produce. D11 LOW: WORKTREE_CONTENT is unanchored, so a second worktrees/<seg>/ escapes the nested-.claude protection. D12 LOW: section 2 of the message states 'Both are now never-additive' of the MCP keys and the shipped code does not do that; the retraction is 60 lines later. D15 LOW: selfConfigHeadline silently drops an alarm with a blank line, which its own comment says it does not. CONFIRMED TRUE: the permissions carve-out does not swallow nested hooks/env (mutation to a bare continue fails the named test), selfConfigHeadline derives from the kinds present in all six shapes, isBlockingControl correct on 36 path shapes with both mutations caught, .mcp.json in BOTH lists with the parity gate catching a one-sided removal, and the suite green at 039ff447. ALL OPEN."}
# {"commit":"c9e3d682","auditor":"code-b (did not write it)","at":"2026-09-19T19:55:00Z","found":1,"note":"shell rail + stop gate. D1 MEDIUM DERIVED: the new prefix-resolution scoping keys on argv0 taken RAW from tokens[0] -- `if (!PREFIX_RESOLVED_TOOLS.has(argv0)) return false` -- with no normalisation of case or path. On Windows, where this runs and where the filesystem is case-insensitive, `SORT --out=x` resolves to the same binary and misses the Set, so the prefix rule is skipped and `--out` (not itself in WRITE_FLAG_LONG, only a prefix of --output) is no longer matched. A leading path such as /usr/bin/sort has the same effect if any earlier stage admits it. This repository has been bitten by case-variant bypasses before -- .Claude/settings.json and six more were ALLOW with no classification at all -- and the guard's own path matcher folds case for exactly this reason while this Set does not. UNMEASURED: I did not run the rail against SORT, so it is derived from the code, and it depends on `sort` being admitted by an earlier stage at all. VERIFIED TRUE: PUSH_REWRITE_SHORT /^-[A-Za-z0-9]*[df][A-Za-z0-9]*$/ covers -d and -f in clusters and glued forms, and d and f are precisely the destructive short options of git push -- -u, -n, -q, -v, -o and -4/-6 carry neither letter, so the cluster rule does not over-block any documented push option; it is scoped to gitOptionTokens(tokens,'push') so an operand branch name containing d or f cannot match, both because the regex is anchored at `-` and because operands are not returned. ALSO TRUE, and worth recording as practice rather than as a defect: this commit corrects three defects its own author shipped in the previous two commits, names the false justification it had given verbatim, labels the new scoping AS an enumeration -- the failure mode that keeps recurring here -- and names the durable repair (a per-tool arity table) rather than claiming the enumeration is one."}
# {"commit":"43672dc","auditor":"code-b (read the diff; did not write it)","at":"2026-09-19T11:05:00Z","found":3,"note":"audit-auto. ALL THREE DERIVED FROM SOURCE, NOT MEASURED -- I read the diff and never ran the tool, which is the measured/derived split I required of every subagent and did not apply to myself. D1 process.exit(hollow.length?1:0) exits 0 when every commit SKIPPED, so a run that proved nothing is indistinguishable from one that proved everything. D2 red accepts status!==0 && pass===0, so a post-revert crash -- an unresolvable import -- reports GATE with no assertion executed, which is CLAUDE.md hollow gate 9 verbatim. D3 the node_modules junction is mklink /J via cmd, unguarded, so on a non-Windows machine deps are absent, every commit reports not-green and is skipped, and the tool still exits 0. Confirming any of these needs a run, which needs machine headroom this box did not have. UNFIXED"}
{"commit":"50470d4","auditor":"blind-subagent (own clone, isolated AGENTBRIDGE_HOME, clean brief)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-19T18:35:00Z","found":8,"note":"watcher: batched process-table query + tri-state alive(). D1 HIGH, NEW AND DESTRUCTIVE, introduced by this commit: sessionEnd used `alive(rec.pid) !== false`, so a pid we could not identify was SIGTERMed. On Windows that is TerminateProcess, no handler, no veto. Demonstrated with a live non-node victim whose pid sat in a stale poll record -- PARENT 8e9908e: ping.exe STILL-RUNNING; COMMIT 50470d4: EXITED code=1. The safe default was applied to the wrong risk: an orphaned poller is visible in --status and bounded, a terminated stranger is neither, and pid reuse reaches this with no probe failure at all. D2 MEDIUM: two mutations uncaught by the claimed 5/5 -- `live.has(pid)` -> true (the node-ness check, which is what limits blast radius, had nothing falsifying it because every fixture used process.pid or 999999, and 999999 is rejected by process.kill before tasklist is consulted) and the pid-less record reading unknown instead of dead. D3 MEDIUM: the cost test PASSED AGAINST THE DEFECT on a cold machine -- it timed the new query shape (138-2008ms cold) as the yardstick for the old one (~150ms stable), so the budget ballooned past the thing it bounded; the auditor watched it spend 39.8s observing the bug and report pass. D4 MEDIUM/LOW: the rival test leaks a real detached supervisor on every failing run, with one from the author own pre-commit run still alive on the machine. D5 LOW: the \"830ms per record / 16620ms\" headline did not reproduce -- measured ~145ms per record and 3163ms for 20, a 5x overstatement carried from an earlier audit instead of re-measured; the FIX is real and large (linear 271->3163ms becomes flat 311->318ms). D6 LOW: JSDoc still declared pidAlive:boolean after it became tri-state. D8 LOW: the new end-to-end tests are Windows-only by construction and pass vacuously on POSIX. VERIFIED TRUE by the auditor: 24/24 in the file, siblings 7/7 13/13 9/9, the claimed mutation 5/5 independently reproduced with hashes, the register-URL loopback pin real. D1-D4 and D6 fixed in 1d639f1; D5 and D8 disclosed there and left open."}
{"commit":"7ca06fc","auditor":"blind-subagent (own clone, isolated AGENTBRIDGE_HOME, clean brief)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-19T19:05:00Z","found":0,"note":"audit-auto: require after.fail > 0 before claiming a load-time GATE. Audited as one of three; NO DEFECT SPECIFIC TO THIS COMMIT, and the auditor called it the strongest of the three. VERIFIED TRUE, independently reproduced: \"3 tests, 3/3 green, against the parent 1 red 2 green\" exact, with the one red being the shrinking-count test and the two greens the positive controls; the quoted pre-fix output reproduced VERBATIM including `gates proven: 1` for a commit whose tests pass without the code; \"scripts/audit-auto.mjs has never had a test\" confirmed by git log; the HOLLOW/hollow counter-spelling fix confirmed live. The auditor also checked deliberately that the new after.fail gate cannot be reached with the -1 sentinel, because `if (!after.ran)` returns UNKNOWN first -- the shape that would have turned this fix into a new false HOLLOW. One correction to the commit disclosure: subjectsOf matches `new URL(...)` as well as `from ...`, verified empirically. The disclosed barrel and dynamic-import gaps were closed later in c5da0c6."}
{"commit":"5f9e1c7","auditor":"blind-subagent (own clone, isolated AGENTBRIDGE_HOME, clean brief, told nothing of prior findings)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-19T21:05:00Z","found":0,"note":"formatCoverage coerces every field it prints. Audited as one of twelve in a single pass; NO DEFECT SPECIFIC TO THIS COMMIT. VERIFIED TRUE by the auditor: the claimed \"auditEscalation 15/15, wiring 4/4, auditLedger 12/12\" reproduced exactly, each file run alone with exit codes taken unpiped; the new test was watched failing against the parent (src/auditLedger.mjs restored to 760b56eb, mutation hash-verified as landed, tests 15 pass 14 fail 1 on the named test A MALFORMED COMMIT RECORD IS REPORTED NOT THROWN, then restored). The pass also established its own baselines at both ends of the twelve-commit span -- 2458/2453/0 at the parent of the first, 2552/2547/0 at the tip -- and found zero pre-existing and zero clone-only failures, so nothing in it rests on an environment phantom."}
{"commit":"c04fdcb","auditor":"blind-subagent (own clone, isolated AGENTBRIDGE_HOME, clean brief, told nothing of prior findings)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-19T21:05:00Z","found":0,"note":"audit-auto: every verdict reaches a counter, plus three corrections to my own claims. Same twelve-commit pass; NO DEFECT SPECIFIC TO THIS COMMIT. The auditor independently re-derived the corrected count at six revisions -- candidates 22, blocking 15, report-only 7 at 50efc72 / 7ca06fc / c04fdcb / a577ba5, drifting to 23/16/7 only at 039ff44 which is another agent commit -- confirming both that the retraction of my earlier \"18\" was right and that the replacement figure is right. It also enumerated every verdict literal the script can emit (real-gate, HOLLOW, LOOSE, NEEDS-MANUAL, collapsed, unknown, error, no-tests, all-new, not-green), confirmed all nine are bucketed with none double-counted, and confirmed every continue in the per-sha loop is preceded by a findings.push. The \"25 candidates\" literal this commit removed from a test comment was confirmed present at the parent and absent here."}
{"commit":"d81e964","auditor":"blind-subagent (own clone, isolated AGENTBRIDGE_HOME, clean brief, told nothing of prior findings)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-19T21:05:00Z","found":0,"note":"SUPERSEDED -- read the found:8 row for this commit first. package.json split by key: a scripts change blocks, a dependency bump does not. Same twelve-commit pass; NO DEFECT SPECIFIC TO THIS COMMIT, and the auditor exercised it on REAL HISTORY rather than on the fixtures the commit shipped. Measured: 8d42ad3, 43672dc, b00d96e, dcc9a98 all scriptsChanged true; 82f75f0, a79a759, 4200904 false; 22f54a1 (root commit, no parent) true, i.e. failing closed. End to end, 8d42ad3 emits the scripts marker and BLOCKS, 43672dc (the npm-script commit this commit names as the outage) blocks, and deps-only commits keep the plain path and are reported without blocking. It further checked that git show is permitted by safeGit rail, so the fail-closed branch is not firing by accident, and that only two consumers read `touched` (check-audit-coverage and claude-stop-gate), both formatters, so the non-path marker breaks nothing downstream. NOTE FOR A LATER READER: 76e98a9, by another agent, has since changed scriptsChanged to a string tri-state and made the marker APPEND rather than substitute; this audit describes the commit as it stood.","superseded_by":"the second independent blind audit of this commit, at 2026-09-20T00:30:00Z, which returned found:8 including two HIGH. Both passes were blind subagents in their own clones; this one is kept because it is a real record of a real pass and because two of its CONFIRMED TRUE items still hold, not because its count stands."}
# ── 2026-09-20: two blind audits of the 22 escaped control commits. ──────────
# READ THE LIMITATION BEFORE READING THE VERDICTS. Both auditors were separate
# agents with their own clone and isolated AGENTBRIDGE_HOME, each given the
# §7.4 proof obligations and NOT the commit subjects. Both independently
# reported that they could not EXECUTE ANYTHING in the clone: our own rail
# refuses `cd`, `git -C`, `Set-Location` and `node` on any non-inherited path.
# So obligations 1, 2 and 6 -- reproduce, independently re-measure, mutate --
# were UNPERFORMED, and every line below is STATIC ANALYSIS. They still found
# seven live fail-opens between them. Full report: docs/AUDIT_FINDINGS_2026-09-20.md
{"commit":"dab3627","auditor":"blind-subagent batch A (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":5,"note":"STATIC ONLY -- could not execute, see header. FAIL. D1 HIGH: mergeQueue rebuilds each job carrying exactly four fields, erasing independence, author_session, author_source, claimed_by_source, satisfies_gate, verdict, recorded_by, recorded_at, finding_refs, evidence_refs. bin/agentbridge.mjs runs mergeQueue before every claim and record, so audit-record refuses EVERY job with 'independence is unknown, not enforced' for a reason unrelated to independence -- the P0-3 write path is unreachable. D3 HIGH: the headline claim is false as wired; the Stop gate imports auditJobsFor/formatAuditJobs only, not mergeQueue or claimJob, and appends to the session's own notice -- byte-identical to what the message says was replaced. Zero references to `agentbridge audits` outside bin/, so the queue exists only when a worker remembers, which is what §7.1 forbids. D10 LOW read-modify-write claim with no lock in a feature built for concurrency. D11 LOW writeQueue rewrites all N rows per call. D12 LOW a corrupt queue line is dropped silently and reads as 'nothing due' exit 0. VERIFIED TRUE: auditIdFor is deterministic and NUL-framed so two pairs cannot collide by concatenation; the claim output carries no subject and assertBlind refuses one -- the auditor confirmed blindness from the receiving end, its own brief carried the eight proofs and no subject. UNFIXED."}
{"commit":"6071798","auditor":"blind-subagent batch A (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":2,"note":"STATIC ONLY. FAIL. D7 MEDIUM: authorSessionFrom uses String.match without /g so it takes the FIRST trailer, and Claude Code appends the real one at the END -- a decoy line earlier in the body wins. Three bypasses of what the message calls 'machine-enforced independence': omit the trailer (author===null, the presence guard short-circuits, no refusal at all -- the nonEmpty(x) && ...x... fail-open shape), type any other --by, or prepend a decoy. D8 MEDIUM: claimant and author ids come from different namespaces -- AGENTBRIDGE_AGENT_ID is `code-b`, authorSession is a `session_...` string -- so author===who can never be true and the refusal is structurally unreachable, while the record says claimed_by_source:'resolved', the stronger marker. VERIFIED TRUE: the trailer extractor is correct on a full URL and returns null in all three negative directions; live claims cannot be taken and stale ones can, with both controls present; the stated reason for not using git %an (it cannot separate three agents on one machine) is correct. UNFIXED."}
{"commit":"8e52c1f","auditor":"blind-subagent batch A (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":0,"note":"STATIC ONLY. PASS (weak) -- satisfiesGate had no production caller at this commit. This is a self-correction of 6071798 and the auditor called the correction right: author-written provenance is capped at `asserted` and can never reach `enforced` whatever bySource says, tested for both the explicit trailer source and the defaulted case. independenceOf is tested over the GENERATED 3x2 matrix rather than the one passing combination -- the auditor called it the strongest test in the set, rule 7 done properly. satisfies_gate is written onto the record rather than left to be re-derived."}
{"commit":"fe25652","auditor":"blind-subagent batch A (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":1,"note":"STATIC ONLY. FAIL. D4 HIGH, AND IT IS THE ONE THAT MATTERS: the 'authoritative' author identity is an environment variable the author sets. identity_source:CREDENTIAL comes from process.env.AGENTBRIDGE_PRINCIPAL_ID and the claimant side from AGENTBRIDGE_SESSION_ID ?? AGENTBRIDGE_AGENT_ID; nothing verifies a credential. So an author produces a fully `enforced`, gate-satisfying SELF-AUDIT by exporting two variables and choosing two different strings. The message says authorship is bound 'from the active authenticated session, which is a thing the author does not get to type' -- it is a thing the author types. Three comments in candidateAuthorship.mjs repeat the claim and no code enforces it. Mitigation today is only that nobody has exported the vars, which is a property of the machine, not the design. VERIFIED TRUE: first-binding-wins is real and BOUND includes candidate_tree_sha so an amend is caught; identity_source accepts only the exact lower-case marker; a record naming neither session nor principal is refused. UNFIXED -- this is P0-5 and src/governor.mjs now refuses `env` as an anchor by name."}
{"commit":"94c4ed9","auditor":"blind-subagent batch A (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":3,"note":"STATIC ONLY -- the red-test claim below is DERIVED, not executed. FAIL. D5 HIGH: shipped with two failing assertions in its own test file. It replaced JOB.DONE with COMPLETED_PASS/COMPLETED_FAIL and left claimJob comparing against the deleted constant (undefined), so 'a DONE job is not reclaimable' and the commit's own new 'THE WRITER MUST BE THE CLAIMANT' both go red. Red at 94c4ed9, still red at cba3c0d, green only at d2df7f3. Measure with three checkouts and read the reported COUNT: expect fail 2, fail 1, fail 0. D9 MEDIUM: the 'candidate must not have moved' fence compares the job against itself as wired -- nothing re-measures the tree at record time, so with the documented flagless usage both sides of both comparisons are the same stored value; it catches a typo, not the attack its own comment cites. D13 LOW: idempotency ignores evidence_refs, so a retry with new evidence silently drops it. VERIFIED TRUE: every other fence is individually correct and separately named-tested, and the idempotent-retry check is deliberately placed before the state fences with the reason stated, which is necessary and correct. UNFIXED."}
{"commit":"cba3c0d","auditor":"blind-subagent batch A (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":0,"note":"STATIC ONLY. PASS (partial): correctly fixes one of the two reds it inherited and leaves the file red -- see 94c4ed9 D5. The fix is the right SHAPE: TERMINAL.includes(state) instead of a named constant, and the replacement test iterates Object.values(JOB) filtered to COMPLETED* so a third terminal state is covered on the day it is added, which is the generated-fixture discipline rule 7 asks for. The CLI `open` filter was fixed in the same commit so the two sites stayed consistent, and the message names its own defect accurately."}
{"commit":"d2df7f3","auditor":"blind-subagent batch A (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":1,"note":"STATIC ONLY. PASS (incomplete). D6 MEDIUM: fixes ONE of two identical Number(null)===0 sites in the same file. Its own message says 'THE SECOND TIME THIS EXACT COERCION HAS BITTEN IN THIS SESSION' and the identical pattern in claimJob, ~130 lines above and in the same diff hunk context, is untouched: claimed_at:null -> Number(null)=0 -> finite -> age becomes an epoch-ms value -> the staleness refusal never fires and A LIVE CLAIM IS STOLEN. The fail-open direction is the opposite of recordAudit's but it is the same bug, and mergeQueue explicitly preserves claimed_at ?? null so null is a shape the store carries by design. The commit also adds NO TEST (0 insertions, 0 deletions, binary diff); the assertion that catches it was written two commits earlier for another purpose. Rule 8: fix the matcher, not the one site the prober reached. VERIFIED TRUE: the recordAudit fix itself is correct and fail-closed across null, undefined, '1000', NaN, Infinity and objects. UNFIXED."}
{"commit":"d6f4808","auditor":"blind-subagent batch C (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":3,"note":"STATIC ONLY -- could not execute, see header. PASS with 3 latent defects. D-A MEDIUM: decideVerify's identity re-check is `key !== null && str(record.key) !== str(key)` with key defaulting to null, so omitting the argument skips the check entirely and a foreign VERIFY_PASSED record is reused -- the nonEmpty(x) && ...x... fail-open shape recorded in memory as this author's recurring bug. D-B MEDIUM: Number(r.exitCode) !== 0, and node sets code=null on death by SIGNAL, so a shard killed by the OOM killer is not counted as failed and any surviving shard with tests carries the run to VERIFY_PASSED -- a forged pass. D-C LOW: shardPlan's more-shards-than-files refusal is skipped when files is omitted. None reachable today because every production caller passes the argument; all three untested in that direction. VERIFIED TRUE, and the auditor called test/verifyCache.test.mjs unusually strong: every identity field shown to move the key ALONE, generated over Object.keys(IDENT); NUL framing asserted by a genuine collision pair rather than by inspection; a FAILED result reused (the half usually forgotten); the heartbeat boundary tested on BOTH sides. D-B FIXED in 11e1615 with tests; D-A and D-C UNFIXED."}
{"commit":"2c91953","auditor":"blind-subagent batch C (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":0,"note":"STATIC ONLY. PASS -- clean. Number(record.heartbeat_at) replaced by an explicit typeof==='number' && Number.isFinite check, pinned by a NAMED assertion (heartbeat_at:null produces /never beaten/) that fails without the change. The auditor noted the author was honest that both spellings refused and only the REASON was wrong."}
{"commit":"472aaf3","auditor":"blind-subagent batch C (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":0,"note":"STATIC ONLY. PASS -- clean. Both PROTECTED_PATHS lists (src/guardSession.mjs and src/policy.mjs) updated in ONE commit, which is what those files' own comments demand, and scripts/verify-run.mjs correctly declared in DEFAULT_ENTRY_POINTS. No defect found."}
{"commit":"aaa72de","auditor":"blind-subagent batch C (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":2,"note":"STATIC ONLY. FAIL. D1 HIGH: the gate's TAP verification was deleted, unannounced. Three refusal branches removed wholesale -- test-run-failed, tap-summary-invalid, tap-counts-refused -- including the reconciliation pass+fail+cancelled+skipped+todo===tests and the cancelled!==0 refusal. `cancelled` is NO LONGER EXAMINED ANYWHERE in the codebase. The replacement checks only exit code and tests>0, and the commit message enumerates the new refusals at length while never mentioning deleting four existing ones. The rule-13 sting: at HEAD those three tokens survive ONLY inside an explanatory comment, so a grep for the control matches its own comment and reports the control present. D2 MEDIUM: put verifyCache.mjs and verifyIdentity.mjs into the Stop gate's import closure without adding them to PROTECTED_PATHS -- the exact omission 472aaf3 had just fixed for three other files -- leaving guardDependenciesProtected red across five commits until 3d1ec7f closed it 22 minutes later. D1 UNFIXED; D2 closed by 3d1ec7f."}
{"commit":"cec127c","auditor":"blind-subagent batch C (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":0,"note":"STATIC ONLY. PASS. Every branch in the START path calls out(), which process.exit(0)s, so there is no fail-open in this commit. The auditor judged the stated reason for withdrawing the detached spawn -- a child inheriting cwd and blocking rmSync on Windows -- coherent and specific, and it is the same mechanism that later turned out to be D4 on 48e77ec."}
{"commit":"0bef662","auditor":"blind-subagent batch C (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":1,"note":"STATIC ONLY. FAIL (fail-open), closed 11 minutes later by 5debd0f. D3 HIGH: shipped `if (admitted.state === VERIFY.FAILED || VERIFY.PARTIAL || VERIFY.TIMED_OUT)` -- the || chain makes the condition never true for exactly the two states that mean nothing was proved, and control falls through to out(null), which is an APPROVAL. The Stop gate approved an unverified turn whenever a produced record came back PARTIAL. The author reproduced it in 5debd0f's own message. VERIFIED TRUE: killSignal:'SIGKILL' on spawnSync with correct reasoning, and it re-reads the record rather than trusting the exit code (rule 4) -- correctly applied. NOT LIVE."}
{"commit":"48e77ec","auditor":"blind-subagent batch C (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":3,"note":"STATIC ONLY -- the orphan symptom is DERIVED from Promise.race semantics plus out()'s process.exit(0) plus spawn({cwd:root}). FAIL. D4 HIGH: replaced a killing spawnSync timeout with Promise.race, WHICH CANCELS NOTHING. On the deadline path the gate printed its refusal and exited 0 with a full `node --test` per shard still running -- up to `concurrency` orphans per timed-out Stop, unbounded across turns, which is exactly the duplicate-suite load the whole 14-commit series exists to remove, rebuilt through a different door. It also reinstates the Windows EPERM that cec127c cited as the reason to withdraw the detached spawn, and rmSync's `force` suppresses ENOENT not EPERM. The commit message asserts the opposite; the heartbeat went stale because the PARENT died, not the children. THIS WAS THE stopGateDeadline TEARDOWN FAILURE OBSERVED THE SAME SESSION AND WRITTEN OFF AS A FLAKE. D5 MEDIUM: admitVerification is computed twice and never read -- the approval is made by an inline state check that does NOT re-verify the identity key, which is the one thing admitVerification adds; safe today only incidentally. D6 LOW: two adjacent comment blocks assert opposite designs ('THIS GATE STARTS NOTHING. NOT EVEN DETACHED.' directly above 'SO THIS GATE RUNS IT'). D4 FIXED in 11e1615+c62b8f5 with tests; D5 and D6 UNFIXED."}
{"commit":"06f5dbc","auditor":"blind-subagent batch C (own clone, isolated AGENTBRIDGE_HOME, clean brief, no commit subjects)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T02:10:00Z","found":2,"note":"STATIC ONLY. PASS with caveat. D7 MEDIUM: narrowing envDigest to three variables fixes a real measured split-cache bug and the stated rule is sound, but treeDigest hashes CONTENT ONLY and never the root path, so after this commit two checkouts with identical content at different paths produce the SAME KEY -- and the override-grant store is keyed on the git-common-dir, so a PASS taken in worktree A can be reused in worktree B where a DIFFERENT GRANT IS LIVE, which does change what the guard tests observe. D8 MEDIUM: no test file exists for verifyIdentity.mjs or verifyRunner.mjs at all, and verifyIdentity holds the tree digest the entire cache-safety argument rests on while being a protected guard-closure member. Also flagged, X1 MEDIUM-HIGH, live and pre-existing: treeDigest parses `git status --porcelain` and a STAGED RENAME is `R  old -> new`, so rel becomes the literal 'old -> new', readFileSync throws, and the body is recorded as 'absent' -- meaning subsequent edits to the renamed file DO NOT CHANGE THE KEY and a cached PASS is reused across an edit, the single failure the module's own header says it cannot have. Same for C-quoted paths. Should return null. VERIFIED TRUE: the bug being fixed is real and specific (CLAUDE_PROJECT_DIR set in a hook environment and not in an interactive shell, so the gate and npm run verify derived different keys for one tree). verifyRunner now has a test file (c62b8f5); D7, D8-for-verifyIdentity and X1 UNFIXED."}
# ── batch B: the only one of the three that could EXECUTE, and it found the worst thing. ──
# It reached a runnable environment through `npm test --prefix`, which is itself the
# finding (E1 below). Its mutation measurements are clean -- isolated temp home set via
# spawnSync env from inside the harness -- but its two FULL-SUITE runs used the operator's
# real AGENTBRIDGE_HOME, because no permitted spelling can set it. It disclosed that itself.
{"commit":"abe2eb7","auditor":"blind-subagent batch B (own clone, clean brief, no commit subjects)","recorded_by":"code-b, TRANSCRIBING ONLY; not my commit and not my audit","at":"2026-09-20T03:20:00Z","found":3,"note":"PASS with defects. EXECUTED, unlike batches A and C. Built a GENERATED MATRIX OF 5,824 COMMANDS from the module's own ALLOWED_FIRST_TOKENS x 8 flag letters x 7 value shapes, diffed parent-vs-head: 14 loosened, 72 tightened. It then EXECUTED all 14 loosened spellings in a temp dir and confirmed none writes a file. Three mutations caught with named assertions and counts (sort's 'o' set emptied -> quotedWriteFlag 8->4/4 and claudeGuard 40->39/1; clusterTakesFile -> false, same 5 named; the undefined-fallback branch disarmed -> 1 red). R1 MEDIUM over-block: clusterTakesFile scans the glued VALUE not just the flag cluster, so `grep -efoo package.json` DENIES -- the `f` inside `foo` matched -- and the refusal blames a side-file flag that is not present, which is rule 18 from the inside. `-e<PATTERN>` glued is the documented POSIX spelling and the likely real casualty. R2 LOW-MED undisclosed loosening: putting `git` in the table with an empty set removed the accidental `-f` refusal from fetch, tag, merge, rebase, pull, cherry-pick, revert, apply; `git tag -f` and `git fetch -f` are the two that matter and the message does not mention it. R4 LOW latent: toolName() normalises but the adjacent PREFIX_RESOLVED_TOOLS check still tests raw argv0. ALSO FOUND, HIGH AND PRE-EXISTING, NOT THIS COMMIT: `uniq in.txt victim.mjs` is ALLOW and OVERWRITES victim.mjs -- demonstrated end to end, exit 0, file contents replaced -- because uniq takes a positional OUTPUT file, so there is no flag for any matcher to see. Same shape as the canonical `sort -o` attack with one fewer moving part, and `uniq` is one of the tools this commit added to the table. UNFIXED, all of it."}
{"commit":"76e98a9","auditor":"blind-subagent batch B (own clone, clean brief, no commit subjects)","recorded_by":"code-b, TRANSCRIBING ONLY; not my commit and not my audit","at":"2026-09-20T03:20:00Z","found":4,"note":"FAIL, 2 HIGH. L1 HIGH: the shallow-clone half is NOT fixed and the stated discriminator is FALSE. Both the message and the comment say '%P distinguishes them -- empty only for a true root'. Measured: git's shallow graft removes parents from `log --format=%P`, so a `clone --depth 1` boundary reports %P empty and takes the ROOT branch; since any real boundary package.json defines executing keys, the verdict is 'changed' -> SCRIPTS_MARKER -> blocking. That is the same unconditional block on every turn in a shallow clone the commit claims to remove, now delivered as a DEFINITE answer rather than an honest 'unknown'. L2 HIGH, the fix opened a hole: `before === undefined` (parent has no package.json) now returns 'unknown', which produces no marker -- so a commit that INTRODUCES package.json with {\"scripts\":{\"pwn\":\"node ./evil.mjs\"}} is NOT FLAGGED, where the parent commit's code flagged it. Deleting package.json behaves the same. The two edits that create and destroy the entire executable surface are the two the new gate declines to judge. L3 MEDIUM: the NUL-framing regression test is HOLLOW -- removing the framing leaves executableSurface 9/9 and auditLedger 16/16 green, because the new code JSON.stringifies values so the fixture's raw separators are escaped and can never collide. The framing IS load-bearing and the auditor built the collision the test cannot: a single script whose NAME contains NUL separators vs two real scripts serialise identically unframed. L4 MEDIUM: two tests are named for a state they never produce -- 'UNKNOWN DOES NOT PRODUCE THE BLOCKING MARKER' builds a fixture whose verdict is 'same', and 'AN UNREADABLE PARENT IS UNKNOWN' fails in the `after` branch instead. So the 'unknown' verdict has NO caller-level test, which is why L1 and L2 both survived. VERIFIED TRUE: the original over-block on a true root is genuinely fixed, and EXECUTING_KEYS is genuinely read (mutating it to ['scripts'] goes red on a named assertion). UNFIXED."}
{"commit":"388accb","auditor":"blind-subagent batch B (own clone, clean brief, no commit subjects)","recorded_by":"code-b, TRANSCRIBING ONLY; not my commit and not my audit","at":"2026-09-20T03:20:00Z","found":4,"note":"FAIL. A1 MEDIUM: the formatCoverage NOTE has ZERO coverage -- suppressing the lines.push cleanly survives all four candidate files (executableSurface 9/9, auditLedger 16/16, deadExports 10/10, auditEscalation 15/15). A3 MEDIUM: the bin/agentbridge.mjs half is ENTIRELY untested -- grep for DIAGNOSTIC ONLY / GATE-SATISFYING / audit-queue across test/, scripts/, hooks/ returns one hit, the production line itself. The diagnosticOnly counter and the three-line warning have never been run by anything. A2 LOW: the NOTE prints a FALSE SENTENCE in a reachable case -- its condition scans ALL commits rather than the ones listed, and the listing is capped at 20, so it can say 'a package.json change above' when nothing was listed above; should read `missing`, not `commits`. A4 LOW: subject and body describe the audit queue while the diff is the satisfiesGate print and the NOTE, and FIVE commits in this range carry the identical message, which makes git log useless for attributing either change. VERIFIED TRUE: satisfiesGate is exported and the fields it reads -- independence, author_source, claimed_by_source -- ARE written by claimJob, so it is not a guard reading a field nothing writes. UNFIXED."}
{"commit":"8059ba1","auditor":"blind-subagent batch B (own clone, clean brief, no commit subjects)","recorded_by":"code-b, TRANSCRIBING ONLY; not my commit and not my audit","at":"2026-09-20T03:20:00Z","found":1,"note":"PASS. The premise was measured DIRECTLY rather than argued: stripNonCode on the interpolated form yields 'lines.push( `` );' with the name GONE, and on the bound form the name SURVIVES; and on the shipped tree classifyExports reports UNWATCHED_EXECUTION_KEYS as production-referenced. The change does exactly what it claims for the reason it claims, and the generalisation in the comment -- the dead-export gate cannot see a name that appears only in a string, so it under-reports wiring and never over-reports -- is correct. B1 MEDIUM: nothing protects it. Reverting to the interpolated form leaves deadExports 10/10 and executableSurface 9/9 green, because the ratchet has slack -- so a future edit that re-interpolates any name silently reclassifies it as test-only again, which is the condition this commit exists to remove. UNFIXED."}
{"commit":"5debd0f","auditor":"blind-subagent batch B (own clone, clean brief, no commit subjects)","recorded_by":"code-b, TRANSCRIBING ONLY; not my commit and not my audit","at":"2026-09-20T03:20:00Z","found":3,"note":"PASS with defects. VERIFIED TRUE: the shard-count clamp is genuinely covered -- mutating it back goes red on three NAMED assertions in stopGateDeadline -- and the described fail-open (one test file, four shards, PARTIAL in 400ms, gate approves) is real and closed. The new state read fails CLOSED, so it is not the presence-guard shape. S2 MEDIUM: admitVerification is now A CONTROL NOTHING CONSULTS -- assigned at two lines and never read; repo-wide the only non-test callers are those two discarded assignments. The function documented as 'May a turn be approved on this record?' and defended at length in the commit body has no production consumer, and the approval now rests on a raw record.state string read off disk. Correct today only incidentally, because decideVerify re-checks the key earlier and the ATTACH branch exits. S3 MEDIUM: the central change is nearly unprotected and NOT by the file the message credits -- disarming `if (state !== VERIFY.PASSED)` survives stopGateDeadline 5/5, stopGateRecovery 6/6, committedDriftNotBlocking 5/5, gateConfigArms 10/10, guardResetBypass 4/4, worktreeAuditDeadlock 9/9 and verifyCache 22/22, and is caught by exactly one named assertion in auditEscalationWiring, a file about something else. S4 LOW: reading record.state raw drops admitVerification's key re-check, which is why S2 matters rather than being cosmetic. UNFIXED."}
{"commit":"11e1615","auditor":"blind-subagent, FRESH (told nothing of prior findings; ran in the main repo with git status reported verbatim at both ends)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T03:40:00Z","found":4,"note":"FAIL. D1 HIGH, AND IT IS AGAINST THE FIX RATHER THAN THE BUG: the gate-side deadline fix -- this commit's own HIGH-severity headline -- shipped with ZERO coverage. The auditor reverted the abort-and-reap to the exact pre-fix state, proved the mutation landed by diff, and test/stopGateDeadline.test.mjs stayed tests 5 pass 5 fail 0. MUTATION ESCAPED. Because `/stop-deadline/` is printed by BOTH deadline branches, so the assertion agrees with the fixed and the broken code alike -- rule 4, a proxy. D2 MEDIUM/HIGH, DERIVED not measured: `node --test` runs each test file in its own subprocess (this repo documents it at test/stopGateDeadline.test.mjs:126), so SIGKILL on the shard supervisor leaves up to one worker per file alive -- and the workers are what hold cwd, the exact EPERM the fix was written for. The orphan class was closed one level deep. The auditor could not measure it: the rail refuses Get-Process and Get-CimInstance. D3 MEDIUM: cancelling makes every shard non-zero so aggregateShards records VERIFY_FAILED, and decideVerify REUSES a completed FAILED result -- so a cancellation would pin a PERMANENT RED on a tree that was never tested. D5 LOW: runShard's exit-code coercion is an uncaught mutation, redundant today only because aggregateShards now catches non-integers. VERIFIED TRUE: the Number(null) forged pass is real and closed, reproduced by restoring the parent expression -> 7 tests, 5 pass, 2 fail with both NAMED assertions firing and actual 'VERIFY_PASSED'; the positive control stays green under that mutation; the matcher was fixed not the value (-0 correctly passes, 0n and boxed Number(0) correctly fail, no bypass found); SIGKILL-not-SIGTERM genuinely asserted; no spawn-after-abort race; 60/60 and 23/23 on neighbours. D1, D2, D3, D5 FIXED in 4baf12b, WHICH IS ITSELF UNAUDITED."}
{"commit":"c62b8f5","auditor":"blind-subagent, FRESH (told nothing of prior findings; ran in the main repo with git status reported verbatim at both ends)","recorded_by":"code-b, WHO WROTE THIS COMMIT and is only transcribing; the audit was not mine","at":"2026-09-20T03:40:00Z","found":1,"note":"PASS with reservations. D4 MEDIUM: the test's failure mode is an INFINITE HANG, not a red. The fake child only emitted close when killed, so any mutation breaking the kill path made `await run` never settle -- the auditor mutated exactly the abort listener and waited 90 seconds before killing it manually. So rule 1 was unsatisfiable for the central defect: it does not fail, it wedges, and in a full-suite run that is the hung suite this whole mechanism exists to avoid. The rail also refuses --test-timeout, so no caller can bound it. VERIFIED TRUE: assertions are named and specific; the spawnFn = spawn default is the right default and production never overrides it, so this is NOT the 'fix present in source, absent in production' shape; the commit's own disclosure -- that it does not prove a real node --test child dies -- is accurate and correctly scoped. FIXED in 4baf12b (the fake now self-closes on an unref'd timer), WHICH IS ITSELF UNAUDITED."}
